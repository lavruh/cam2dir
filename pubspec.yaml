name: camera_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.9.3
  # image_picker: ^0.8.4
  path_provider: ^2.0.3
  intl: ^0.17.0
  video_player: ^2.2.3
  get: ^4.3.8
  flutter_fancy_tree_view: 
    git:
      url: https://github.com/mbaumgartenbr/flutter_tree_view.git
      ref: main
  photo_view: ^0.13.0
  shared_preferences: ^2.0.8
  notes_on_image:
    path: /home/lavruh/AndroidStudioProjects/NotesOnImage



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true